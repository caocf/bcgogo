update  `message_template` set content='尊敬的会员$!{cardOwnerName}，您于$!{consumeDate}消费#if(${consumeItems}!=$null && ${consumeItems.isEmpty()}==false)项目：#foreach(${item} in ${consumeItems.keySet()})$!{item}$!{consumeItems.get(${item})}次#if($velocityHasNext)，#end#end；#end#if(${consumeAmount}>0)使用卡上金额${consumeAmount}元；#end#if(${remainItems}!=$null && ${remainItems.isEmpty()}==false)剩余项目：#foreach(${item} in ${remainItems.keySet()})$!{item}$!{remainItems.get(${item})}次#if($velocityHasNext)，#end#end#if(${remainAmount}>0)；#else。#end#end#if(${remainAmount}>0)卡上余额${remainAmount}元。#end${shopName}店敬启${shopMobile}。' where id=10000010001000021;

